# AI Corporation Codex 工作规则

本文件是 Codex 的唯一仓库入口，只保留启动顺序和不可违反的门禁。详细规则以链接文档为唯一来源。

## 开始前

1. 阅读 [`PROJECT_STATUS.md`](PROJECT_STATUS.md)，确认当前阶段、当前任务和活跃阻塞。
2. 实现、修复或 Milestone 交付只能执行其中指向的当前[任务合同](docs/06-engineering/task-units/)；合同未达到“就绪”不得实施。
3. 按[文档中心的任务路由](docs/README.md)只读取当前任务直接涉及的产品、协议、模块、数据、安全和 UI 文档。
4. 阅读[统一验收标准](docs/06-engineering/Acceptance-Standard.md)中适用层级；任务合同还必须符合[任务单元规范](docs/06-engineering/Task-Unit-Standard.md)。

## 实施门禁

- 当前工作必须来自用户明确要求或 `PROJECT_STATUS.md`；不得静默扩大任务范围。
- 发现产品文档、设计、协议、Schema、安全、UI、验收或任务合同存在任何可能改变执行结果的歧义、冲突或缺口时，必须在修改权威文档或实现前停止，并请求用户决策。
- 决策请求必须列出原文与歧义点、受影响范围、2–3 个可执行方案、各方案的目标行为、优缺点、迁移与验收影响，以及推荐方案和理由；用户未明确选择前，不得自行选型、替用户改写需求或进行推测性实现。
- 安全硬限制高于产品范围，产品范围高于领域/协议，领域/协议高于模块实现和 UI 建议；冲突无法消解时停止并请求决策。
- 只修改当前任务所需文件，保留用户和其他任务的改动。
- 协议、迁移、状态机、安全策略、CI 和项目状态属于共享冲突区，必须按任务合同串行集成。
- 状态机、协议、数据库或安全边界变化必须同步更新其唯一权威文档与测试。
- 用户可见变更必须遵守 [UI/UX 文档中心](docs/07-ui/README.md)路由的流程、状态、设计系统和 [UI 专项验收](docs/07-ui/UI-Acceptance.md)。
- Approval、Recovery、预算耗尽和验收失败不得隐藏精确资源、风险、证据或后果。

工程实现遵守[工程与编码规范](docs/06-engineering/Engineering-Standards.md)。Milestone 范围和退出条件只由 [MVP 开发计划](docs/06-engineering/MVP-Plan.md)定义。

## 完成门禁

- 所有工作按[统一验收标准](docs/06-engineering/Acceptance-Standard.md)验收并提供直接证据。
- 模型自评、代码已生成、构建成功或进程存活不能代替目标交付物的对应层级验收。
- 适用检查失败、P0/P1 未解决、安全边界未验证、交付物或证据不完整时不得宣称完成。
- 任务通过只关闭自身，不自动代表相邻功能或 Milestone 通过。
- 无法执行的检查必须报告原因、风险和补验方式；部分完成必须列出剩余工作。

## 结束后

1. 更新 `PROJECT_STATUS.md` 的当前结论、有效验证摘要、活跃阻塞和下一步；不得追加历史时间线。
2. 运行适用工程检查，至少包括 `pnpm check:status`、`pnpm check:task-units` 和变更对应测试。
3. 确认设计、协议、Schema、迁移、测试和实现一致。
4. 向用户报告任务单元 ID、结果、验证证据、未执行验证、已知限制和下一步。
