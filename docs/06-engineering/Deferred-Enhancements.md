# 简化与后续增强清单

| 属性     | 内容                                                                   |
| -------- | ---------------------------------------------------------------------- |
| 文档角色 | 计划（Planning）                                                       |
| 适用范围 | 为加快当前交付而主动缩小、简化或延期，但未来仍需补齐或丰富的能力与验收 |
| 最近更新 | 2026-08-14                                                             |

## 1. 使用规则

本清单是项目所有“先简化、以后补齐”事项的唯一入口。任务合同中的普通非范围不自动成为未来承诺；只有确认未来仍需实现或丰富的内容才登记在这里。

- 每项使用唯一 `DE-xxx` 编号，说明当前让步、目标能力、来源、计划落点和关闭条件；
- 当前任务采用简化方案时，必须先登记，再在任务合同“简化与后续增强”章节引用编号；
- 后续任务合同认领某项时，把“计划落点”换成具体任务单元 ID，并把状态改为“已安排”；
- 只有目标能力通过对应任务验收后才能改为“已补齐”；仅创建任务、写代码或测试一部分不能关闭；
- 取消某项必须由用户明确决定，并记录替代方向或取消原因；不得因为时间久远、里程碑关闭或当前实现可用而静默删除；
- 每次阶段复盘和新 Milestone 拆分任务时都要检查本清单，决定哪些项目进入下一阶段。

受控状态：`待安排`、`已安排`、`已补齐`、`已取消`。

## 2. 当前清单

### DE-001 增加 Responses Adapter

| 字段     | 内容                                                                                                             |
| -------- | ---------------------------------------------------------------------------------------------------------------- |
| 状态     | 待安排                                                                                                           |
| 当前让步 | M2-TU-04 为了使用当前真实 Provider 低风险验收，只交付 Chat Completions 非流式 Adapter。                          |
| 目标能力 | 新增 Responses Adapter，与 Chat Adapter 并存；不替换现有 Adapter，通用协议继续保持 dialect-neutral。             |
| 来源     | M2-TU-04；用户确认的 Responses 前向兼容门禁                                                                      |
| 计划落点 | v0.2 Provider 扩展任务                                                                                           |
| 关闭条件 | Responses Adapter 具备独立协议映射、双 Adapter 共存路由、错误与 usage 归一化测试，并通过真实兼容 Provider 验收。 |

### DE-002 建立独立的 streaming 规范化事件协议

| 字段     | 内容                                                                                                                             |
| -------- | -------------------------------------------------------------------------------------------------------------------------------- |
| 状态     | 待安排                                                                                                                           |
| 当前让步 | M2-TU-04 只做非流式生成，没有交付 Chat streaming 或 Responses streaming。                                                        |
| 目标能力 | 另建与 API dialect 解耦的 streaming 事件协议和任务；Chat 与 Responses 分别适配该协议。                                           |
| 来源     | M2-TU-04；用户确认的 Responses 前向兼容门禁                                                                                      |
| 计划落点 | v0.2 Provider/交互增强任务                                                                                                       |
| 关闭条件 | 独立事件协议、背压/取消/错误/断线语义和至少两种 dialect Adapter 通过自动与真实窗口验收；不得以 Chat streaming DTO 作为公共基础。 |

### DE-003 丰富 Plan Review 编辑能力

| 字段     | 内容                                                                                                                                         |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 状态     | 待安排                                                                                                                                       |
| 当前让步 | M2-TU-08 只允许编辑标题、目标、说明、优先级、验收标准、依赖和删除 Task；不能新增 Task 或编辑输入输出、能力、工具、预算、权限、里程碑和风险。 |
| 目标能力 | 提供完整且受验证保护的复杂 DAG 编辑，包括新增 Task 和当前被锁定的计划字段。                                                                  |
| 来源     | M2-TU-08；MVP Plan 的 `P2 / v0.2`“复杂 DAG 编辑”                                                                                             |
| 计划落点 | v0.2 复杂 DAG 编辑任务                                                                                                                       |
| 关闭条件 | 支持上述字段的新增/编辑、引用修复、版本化、冲突处理和完整 Plan Validation，并通过人工 Plan Review 验收。                                     |

### DE-004 丰富动态组队与 Agent 能力匹配

| 字段     | 内容                                                                                                         |
| -------- | ------------------------------------------------------------------------------------------------------------ |
| 状态     | 待安排                                                                                                       |
| 当前让步 | M3-TU-01 使用三个固定 Executor 能力组、固定模板和确定性映射，不生成 Specialist，也不学习 Agent 能力。        |
| 目标能力 | 在保持职责分离和安全硬限制的前提下，支持更丰富的 Agent 模板、动态角色组合、Specialist 和能力匹配增强。       |
| 来源     | M3-TU-01；Organization Engine；MVP Plan 的 `P2 / v0.2`“Agent 能力学习增强”                                   |
| 计划落点 | v0.2 Organization Engine 增强任务                                                                            |
| 关闭条件 | 动态组合与能力匹配规则有明确协议、可解释结果、稳定回退和真实项目验收，且不会让 Executor 成为自身唯一 Judge。 |

### DE-005 从单个首任务扩展到持续调度

| 字段     | 内容                                                                                                     |
| -------- | -------------------------------------------------------------------------------------------------------- |
| 状态     | 待安排                                                                                                   |
| 当前让步 | M3-TU-03 每次开始只认领一个最高优先级首任务，不继续认领其他 Ready Task，也不处理任务完成后的下一轮调度。 |
| 目标能力 | Scheduler 在并发槽位、依赖和审批约束下持续认领 Ready Task，并在任务完成或状态变化后安全触发下一轮。      |
| 来源     | M3-TU-03 用户决策第 3 项；Scheduler 完整流程                                                             |
| 计划落点 | Milestone 3 后续 Scheduler/Agent Runtime 任务                                                            |
| 关闭条件 | 多任务调度、并发领取竞争、依赖解锁、人工等待和重启恢复均有直接证据，并完成 Milestone 3 连续闭环验收。    |

### DE-006 补齐 Scheduler 完整评分

| 字段     | 内容                                                                                                       |
| -------- | ---------------------------------------------------------------------------------------------------------- |
| 状态     | 待安排                                                                                                     |
| 当前让步 | M3-TU-03 只按用户优先级、人工同级优先、创建时间和 Task ID 排序，没有关键路径、解锁数、等待时间和风险惩罚。 |
| 目标能力 | 实现 Scheduler 设计中的基础硬约束与可解释评分，覆盖关键路径、解锁收益、等待老化和风险等待惩罚。            |
| 来源     | M3-TU-03 用户决策第 5 项；Scheduler；MVP Plan Milestone 3                                                  |
| 计划落点 | Milestone 3 后续 Scheduler 评分任务                                                                        |
| 关闭条件 | 评分权重和并列规则经用户确认，确定性、饥饿避免、风险/审批边界与 UI 解释均通过验收。                        |

### DE-007 增加 Milestone 2 单条连续端到端回归

| 字段     | 内容                                                                                                         |
| -------- | ------------------------------------------------------------------------------------------------------------ |
| 状态     | 待安排                                                                                                       |
| 当前让步 | M2-TU-09 为快速完成阶段验收，采用各任务直接证据汇总，没有新增一条从 Key 设置到 Plan 批准的未中断自动化旅程。 |
| 目标能力 | 增加一条使用安全测试凭据或受控 Mock 的连续回归，覆盖 Provider 设置、Goal、规划、验证、编辑和批准。           |
| 来源     | M2-TU-09 用户选择的方案 B；MVP Plan Milestone 2 验收说明                                                     |
| 计划落点 | Milestone 6 内测发布前的全链路回归任务                                                                       |
| 关闭条件 | Windows/macOS 最终包在同一隔离数据集上跑通单条连续旅程，失败点可定位，且不泄露 Key 或项目数据。              |

## 3. 当前任务使用的让步

### DE-008 将 Run 候选内容转为正式 Artifact

| 字段     | 内容                                                                                                                                                   |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 状态     | 待安排                                                                                                                                                 |
| 当前让步 | M3-TU-04 只把模型结果保存为可查看、可恢复的候选内容，不创建正式 Artifact 或 Artifact Version。                                                         |
| 目标能力 | Artifact 任务读取可信候选内容，创建带版本、来源、完整性和生命周期的正式 Artifact，并把 Run 从 `PRODUCED` 推进到 `SUCCEEDED`、Task 推进到 `VERIFYING`。 |
| 来源     | M3-TU-04 用户决策 2A、3A；MVP Plan Milestone 3 的 Artifact 独立交付                                                                                    |
| 计划落点 | Milestone 3 后续 Artifact 任务                                                                                                                         |
| 关闭条件 | 候选内容原子转为正式 Artifact Version，可信引用由软件生成，重试/崩溃不产生重复版本，旧版本保留并通过真实窗口验收。                                     |

### DE-009 为 Agent Runtime 接入上游 Artifact 输入

| 字段     | 内容                                                                                                                              |
| -------- | --------------------------------------------------------------------------------------------------------------------------------- |
| 状态     | 待安排                                                                                                                            |
| 当前让步 | M3-TU-04 只运行不依赖上游 Task 输出或 Workspace 文件的首任务；只发送 Goal 摘要、Task 合同、Agent 职责、安全规则和输出要求。       |
| 目标能力 | 在 Artifact 边界完成后，按 Task 输入引用加载可信上游 Artifact Manifest/内容，并执行大小限制、来源标记和提示注入隔离。             |
| 来源     | M3-TU-04 用户决策 6A；Agent Runtime 执行上下文                                                                                    |
| 计划落点 | Milestone 3 Artifact 输入接入任务                                                                                                 |
| 关闭条件 | 必需/可选输入解析、缺失阻断、大小裁剪、来源追踪、恶意内容隔离和重启恢复通过自动与人工验收；不得绕过 Workspace/Artifact 权限边界。 |

### DE-010 执行 Task 声明的工具

| 字段     | 内容                                                                                                                                                 |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 状态     | 待安排                                                                                                                                               |
| 当前让步 | M3-TU-04 允许带 `requiredTools` 的 Task 生成候选内容，但工具不可用且不会执行；软件不读取、写入或修改文件，并在 UI 明示工具操作仍未执行。              |
| 目标能力 | 通过独立工具运行边界执行经验证和授权的工具调用，覆盖审批、权限、输入输出、结果回填、幂等、取消、超时、失败恢复和审计，不相信模型声称的工具执行结果。 |
| 来源     | M3-TU-04 用户选择方案 A；Agent Runtime 与 Tool Protocol                                                                                              |
| 计划落点 | Milestone 3 后续 Tool Runtime 任务                                                                                                                   |
| 关闭条件 | 工具只能由可信 Main/Native 边界执行，模型不能伪造调用或结果；文件与进程权限、审批和人工真实窗口验收全部通过。                                        |

M3-TU-04 使用 `DE-008`、`DE-009` 和 `DE-010`。M3-TU-03 使用的 `DE-005`、`DE-006` 仍未补齐；当前模型运行任务也不代表持续调度、完整评分、正式 Artifact 或真实工具执行已经交付。

### DE-011 从单员工扩展到多员工协作

| 字段 | 内容 |
| --- | --- |
| 状态 | 待安排 |
| 当前让步 | M7-TU-01 只验证一名 Pi 员工，不做自动组队、员工互相委派和公司级调度。 |
| 目标能力 | 根据任务复杂度决定一名或多名员工，支持员工独立模型、技能、上下文、分工、交接和用户可见的协作过程。 |
| 来源 | 产品重启说明；M7-TU-01 |
| 计划落点 | 下一 Pi Milestone，具体优先级由用户决定 |
| 关闭条件 | 简单任务不会被强制组队，复杂任务可由多名员工完成真实分工，过程和最终责任清楚，并通过安装包人工验收。 |

### DE-012 从安全演示工具扩展到真实工作工具

| 字段 | 内容 |
| --- | --- |
| 状态 | 已补齐 |
| 当前让步 | M7-TU-01 只接一个不访问用户文件、进程或外部服务的安全演示工具。 |
| 目标能力 | 按任务类型接入工作区文件、编码、图片、视频、文档、PPT 等真实工具，并只对高风险动作询问用户。 |
| 来源 | 产品重启说明；M7-TU-01 |
| 计划落点 | M8-TU-01 已交付首个文本工具闭环；其余工具由 DE-014 继续跟踪 |
| 关闭条件 | 至少一种真实任务可以用工具产出可验收交付物，普通操作顺畅，高风险操作准确确认，失败和副作用可见。 |

### DE-013 增加附件和多模态输入

| 字段 | 内容 |
| --- | --- |
| 状态 | 待安排 |
| 当前让步 | M7-TU-01 只接受文字任务，不处理附件、图片、音频或视频输入。 |
| 目标能力 | 用户可以在对话中添加附件，员工按模型和技能能力读取、处理并交付相应内容。 |
| 来源 | 产品重启说明；M7-TU-01 |
| 计划落点 | 下一 Pi Milestone，具体优先级由用户决定 |
| 关闭条件 | 附件选择、能力判断、内容传递、大小和错误处理、过程展示及真实交付物均通过安装包人工验收。 |

M7-TU-01 使用 `DE-011`、`DE-012` 和 `DE-013`。这些是先证明单员工好用而做的阶段性简化，不代表多员工、真实工具和附件被取消。

### DE-014 从工作区文本工具扩展到更多真实工具

| 字段 | 内容 |
| --- | --- |
| 状态 | 已安排 |
| 当前让步 | M8-TU-01 只提供目录查看、UTF-8 文本读取、创建和修改，不提供删除、改名、命令、代码检查、图片、视频、Office 或其他二进制工具。 |
| 目标能力 | 按真实任务价值逐步增加编码检查、受控命令、图片、视频、文档、PPT 和其他内容工具，同时保持普通操作顺畅、高风险动作准确确认。 |
| 来源 | 产品重启说明；M8-TU-01 |
| 计划落点 | M9-TU-01 交付首个真实编码命令闭环；图片、视频、文档、PPT 等继续后续拆分 |
| 关闭条件 | 对应工具各自具备明确权限、取消、失败、恢复、过程展示和真实交付物验收；不能用一个文本工具冒充所有任务类型已支持。 |

M8-TU-01 认领 `DE-012` 的首个真实工作工具闭环，并使用 `DE-014` 记录仍未提供的更多工具。

### DE-015 增加强命令隔离

| 字段 | 内容 |
| --- | --- |
| 状态 | 待安排 |
| 当前让步 | M9-TU-01 为了先把编码任务做得好用，完整命令在用户按任务授权后以当前 OS 账户运行；工作目录默认在工作区，但没有容器或 OS 级强隔离，项目脚本仍可能访问工作区外内容。 |
| 目标能力 | 在不破坏常用编码命令、跨平台体验和实时输出的前提下，提供可以真实限制文件、进程和网络影响范围的隔离执行环境。 |
| 来源 | M9-TU-01 用户选择的任务级完整 shell 方案；Threat Model 风险接受 |
| 计划落点 | v0.2 命令隔离专项，具体技术方案以后单独决策 |
| 关闭条件 | Windows 和 macOS 都能证明命令不能越出声明边界，取消与资源限制可靠，常见项目测试和构建可用，并通过安全攻击集及真实安装包人工验收。 |
