/**
 * Package marker for the dependency-free domain layer.
 *
 * Domain types and state machines will be added by their owning milestones.
 */
export const DOMAIN_PACKAGE_VERSION = "0.1.0" as const;
